// import moment from "../../node_modules/moment";
// import moment from "moment";

const util = {

  convertToDate: function(date) {
    console.log(date);
  },

  sortByDate: function(sortby) {
    console.log(sortby);
    // var byDate = arrayOfObjects.slice(0);
    // byDate.sort(function(a,b) {
    //   return a.born - b.born;
    // });
    // console.log('by date:');
    // console.log(byDate);
  }

};

export default util;
